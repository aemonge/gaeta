---
description: Approve the latest pending proposal
agent: evolution
---
Resolve gaeta launcher in this order:
1. `./gaeta` when present in current project root.
2. `gaeta` from PATH.

Run `<gaeta-launcher> proposal approve . latest`.

Then reply with:
- approved proposal path,
- effective decision,
- expected implementation scope now unlocked,
- first execution slice to implement next.
