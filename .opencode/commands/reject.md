---
description: Reject the latest pending proposal with reason
agent: evolution
---
Resolve gaeta launcher in this order:
1. `./gaeta` when present in current project root.
2. `gaeta` from PATH.

Run:

`<gaeta-launcher> proposal reject . latest "Rejected via /reject; revise scope, risks, and validation before resubmission."`

Then reply with:
- rejected proposal path,
- rejection rationale,
- required revisions,
- the exact resubmission checklist for `/propose`.
