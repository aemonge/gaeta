---
description: Capture pause snapshot for next resume
agent: build
---
Resolve gaeta launcher in this order:
1. `./gaeta` when present in current project root.
2. `gaeta` from PATH.

If neither launcher exists, stop and report: `gaeta not found` (recommended fix: run `make build` in gaeta repo and ensure `gaeta` is in PATH).

Run `<gaeta-launcher> pause`.

If command fails due to uninitialized workflow files, run `<gaeta-launcher> init .` and retry.

Then update `docs/.gaeta/pause.md` by replacing the placeholder bullets in these sections with concise, session-accurate content:
- `## Project update`
- `## Conversation summary`
- `## Attempts and outcomes`
- `## Decisions`
- `## Frozen items`

Keep existing sections generated by `gaeta pause` intact (`Current phase`, `Next step`, sprint items, blockers, backlog).

Finally reply with a concise pause message containing:
- current phase,
- next step,
- top pending sprint items,
- blockers,
- key decisions,
- frozen items,
- files updated.

Sandbox visibility rule:
- If you reference verification outside sandbox-visible paths (for example host-home files under `~/.config/*`), state that host-side verification is required and provide exact commands.
