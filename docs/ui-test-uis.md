# UI Test-UIs Baseline

This file freezes the currently approved base style for `forge test-uis` so future iterations can compare against a stable reference.

## Baseline Snapshot

- Command: `forge test-uis 3`
- Name: `Control Rich`
- Intent: calm, concise control-room output with strong table readability.

## Iteration Path

- Experimental follow-up: `forge test-uis 7` (`Rich Iteration A`)
- Focus areas:
  - color tuning
  - total table width fit
  - column width caps and clipping
  - spacing density

## Review Loop

1. Keep `3` stable as baseline.
2. Iterate in `7`.
3. Promote `7` into `3` only after visual approval.
